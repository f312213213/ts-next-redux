import * as counter from './counter'
import * as app from './app'

const index = {
  counter,
  app
}

export default index

export const root = 'https://f-counter-api.vercel.app'

// export const root = 'http://127.0.0.1:8000'
